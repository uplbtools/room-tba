# --- Editor / admin auth ----------------------------------------------------
# Set ADMIN_PASSWORD_HASH to a bcrypt hash of your admin password.
# Generate with: bun run hash-password "your-password"
ADMIN_PASSWORD_HASH=

# Random secret used to sign the admin session cookie. Anything 32+ chars works.
# Generate with: openssl rand -base64 32
SESSION_SECRET=
